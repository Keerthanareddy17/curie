# Copy to .env.local for local overrides. Not needed for local dev against
# `python -m curie serve` on the default port 8000 (see src/lib/api.ts).
#
# In Vercel, set this to the deployed Render backend's public URL, e.g.:
#   VITE_API_BASE_URL=https://curie-backend.onrender.com
VITE_API_BASE_URL=
